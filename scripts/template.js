function getPokeCard() {
    return `<div class="card-header">${pokeJson.name}</div>
                <div class="card-body">
                    <p>Some quick example text to build on the card title and make up the bulk of the
                        card’s content.</p>
                </div>
            <div class="card-footer ">Footer</div>`
}